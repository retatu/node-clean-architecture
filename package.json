{
  "name": "clean-architecture",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --noStackTrace --silent --passWithNoTests",
    "test:unit": "npm test -- --watch -c jest-unit-config.js",
    "test:integartion": "npm test -- --watch -c jest-integration-config.js",
    "test:staged": "npm test -- --findRelatedTests",
    "test:ci": "npm test -- --coverage"
  },
  "author": "",
  "license": "ISC",
  "standard": {
    "env": [
      "jest"
    ]
  },
  "devDependencies": {
    "husky": "^4.3.8",
    "jest": "^27.3.1",
    "lint-staged": "^12.1.2",
    "standard": "^16.0.4"
  },
  "dependencies": {
    "bcrypt": "^5.0.1",
    "express": "^4.17.1",
    "mongoose": "^6.0.13",
    "validator": "^13.7.0"
  }
}
